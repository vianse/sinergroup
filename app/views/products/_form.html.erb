<%= render "header_admin" %>
<div class="row">
  <div class="col-md-1 sidebar-line"></div>
  <div class="col-md-10 sidebar-line">
<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Código" %><br>
    <%= f.text_field :codigo, :class=> "form-control" , :required => true %>
  </div>
  <div class="field">
    <%= f.label "Descripción" %><br>
    <%= f.text_field :descripcion, :class=> "form-control", :required => true %>
  </div>
  <div class="field">
    <%= f.label :costo %><br>
    <%= f.text_field :costo , :class=> "form-control" , :required => true%>
  </div>
  <div class="actions">
    <%= f.submit "Guardar", :class=>"btn btn-success" %>
  </div>
<% end %>
</div>
<div class="col-md-1 sidebar-line"></div>
</div>